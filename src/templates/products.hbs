<ul class="products_list">
  {{#each products}}
    <li>
        <img class="image" src="{{image}}" alt="{{name}}">
        <h2>{{name}}</h2>
        <p>{{description}}</p>
        <p>{{price}}</p>
    </li>
  {{/each}}
</ul>